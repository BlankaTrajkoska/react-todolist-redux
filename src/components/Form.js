import React from "react";

const Form = () => {
  const itemAdded = () => {};
  return (
    <div>
      <input type="text" />
      <button onClick={itemAdded}>Add</button>
    </div>
  );
};

export default Form;
